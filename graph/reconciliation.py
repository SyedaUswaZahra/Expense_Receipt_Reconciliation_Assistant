state=graph_state, extraction_chain=extraction_chain, normalization_chain=normalization_chain, matching_agent=matching_agent, report_chain=report_chain, graph=StateGraph(ReconGraphState, receipts=final_state.get("receipts", receipt=self.extraction_chain.extract(image_path, transactions=final_state.get("transactions", pairs=[], pair=self.matching_agent.match(receipt, flags=final_state.get("flags", review_status=final_state.get("review_status", status==, report_text=self.report_chain.generate(state, final_state=self.graph.invoke(initial_state, match_pairs=final_state.get("match_pairs", report=final_state.get("report"